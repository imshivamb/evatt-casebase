# Local dev — copy to .env.local
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Vercel: set NEXT_PUBLIC_API_URL in the dashboard (Production + Preview) to your
# Render API URL, then redeploy. If missing at build time, the app falls back to
# 127.0.0.1:8000 and production will call localhost.
